<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('https://images.unsplash.com/photo-1525268323446-0505b6fe7778?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60');">
  <div class="container">
    <h1 class="card-title"><%= @cocktail.name %></h1>
  </div>
</div>



<div class="container ">
    <div class="container">
      <div class="d-flex justify-content-end mt-5">
        <div class="row w-80">
          <div class="card" style="width: 18rem;">
            <div class="card-body">
              <% @cocktail.doses.each do |dose|  %>
                <%= link_to 'delete dose', dose_path(dose), method: :delete  %>
                  <%# @cocktail.ingredients.each do |i|  %>
                  <p class="card-text"><%= dose.ingredient.name %></p>
                  <h6 class="card-subtitle mb-2 text-muted"><%= dose.description %></h6>
                <% end %>
              <%# end %>
              <a href="#" class="card-link"><%= link_to 'add new doses',new_cocktail_dose_path(@cocktail) %></a>
            </div>
          </div>
        </div>
      </div>
              <%= link_to 'Back', root_path, class: 'btn btn-primary'  %>
    </div>
</div>
